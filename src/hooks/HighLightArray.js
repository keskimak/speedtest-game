



function HighLightArray(array){

    let speed = 1; //Speed is the frequency, speed will decrease --> esim speed * 1,1 tms

    for (let index = 0; index < array.length; index++) {
        const buttonToBeHighlighted = array[index];

        //ButtonComponent{ isHighlighted: true }
        //This should maybe do only the highlighing and the speed might be in the game screen? 
        
    }

    return number;   
}

export { HighLightArray }